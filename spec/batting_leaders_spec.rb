require 'spec_helper'

describe BattingLeaders do
  it 'has a version number' do
    expect(BattingLeaders::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
